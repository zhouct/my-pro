<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<h1>Hello World</h1>
<div class="nav-open">一级标题</div>
<div class="nav-close">二级标题</div>
<style>
    .nav-open {
        display: inline-block;
        width: 0px;
        height: 0px;
        border-bottom: none;
        border-top: 6px solid rgba(255, 255, 255, .7);
        border-left: 6px solid transparent;
        border-right: 6px solid transparent;
        float: right;
        position: relative;
        top: 5px;
    }

    /* 菜单折叠向上的箭头 */
    .nav-close {
        display: inline-block;
        width: 0px;
        height: 0px;
        border-bottom: 6px solid #fff;
        border-top: none;
        border-left: 6px solid transparent;
        border-right: 6px solid transparent;
        float: right;
        position: relative;
        top: 5px;
    }
</style>
</body>
</html>